# Operuis
is a Java based application that injects Frameworks and Tweaks to the local meta quest 1/2/3 system using either an IP Address or name of vr. Although in every meta quest has a SSH and PSH password but JKA doesn't have password. the SSH or PSH is recommended and more secure, but JKA is NOT recommended and if not operated carefully, both the connection and the VR System may get bricked and not working ever and that is why SSH and PSH is recommended. The service for the SSH/PSH is a driver called WMCH.dsx. If you dont want java and instead do .bat , that will be on next update v2!



